# =============================================================================
# ServiceHub — Environment Variables
# =============================================================================
# How to use:
#   1. Copy this file:  cp .env.example .env
#   2. Edit .env with your values (optional — defaults work for local dev)
#   3. Start services:  docker-compose up --build
#
# IMPORTANT: The .env file is Git-ignored. Never commit real credentials.
# =============================================================================

# --- Database Configuration ---
POSTGRES_URL=jdbc:h2:mem:tempdb # Once we have a DB, we can use jdbc:postgresql://HOST:5432/servicehub
POSTGRES_USER=sa # Once we have a db servicehub
POSTGRES_PASSWORD=servicehub_pass

# --- Spring Boot Configuration ---
SPRING_PROFILES_ACTIVE=prod # use dev for local development
JWT_SECRET=localDevSecretKeyForJWT_CHANGE_IN_PRODUCTION_1234567890

# --- Server Configuration ---
SERVER_PORT=8080
